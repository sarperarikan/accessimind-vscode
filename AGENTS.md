# AccessiMind Agent Notes

Before making changes in this repository, read `PROJECT_MEMORY.md` first.

Priority order for project context:
1. `PROJECT_MEMORY.md`
2. `package.json`
3. `src/extension.ts`
4. The specific files related to the active task

Rules for future sessions:
- Prefer current source code over older documentation when they conflict.
- Expect a dirty git worktree and avoid reverting unrelated changes.
- Treat browser integration and companion extension code as active product areas.
- Update `PROJECT_MEMORY.md` when major behavior or architecture changes.
